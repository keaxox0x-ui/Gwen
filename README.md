# Study Dashboard

A simple static academic dashboard for GitHub Pages.

## Files
- `index.html` — page structure
- `style.css` — UI/design
- `app.js` — functionality and localStorage data

## Run
Upload all four files to the same GitHub Pages repository. No server or database is required.

Data is stored in the browser using `localStorage` under `studyDashboardData`.
